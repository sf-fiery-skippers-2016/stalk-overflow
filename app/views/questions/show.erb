<%= erb :"/users/_question", layout: false, locals: {question: @question} %>
<ul>
	<% @question.answers.each do |answer| %>
		<%= erb :"/questions/_answer", layout: false, locals: {answer: answer} %>

	<% end %>
</ul>
